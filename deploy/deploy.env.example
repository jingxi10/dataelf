# 复制为 deploy.env（不要提交到仓库），填入你自己的真实值

# 域名（前端同域名部署，后端通过 /api 反代）
DOMAIN="your-domain.com"

# 数据库（外部 MySQL）
DB_HOST="your-mysql-host-or-ip"
DB_PORT="3306"
DB_NAME="ai_data_platform"
DB_USER="your_db_user"
DB_PASS="your_db_password"

# Redis（外部 Redis）
REDIS_HOST="your-redis-host-or-ip"
REDIS_PORT="6379"
REDIS_PASS=""   # 无密码留空

# 邮件（可选，不用就留空；如果后端启动时报 mail health 失败，可在后端配置里关闭 mail health）
MAIL_HOST="smtp.example.com"
MAIL_PORT="465"
MAIL_USERNAME=""
MAIL_PASSWORD=""
MAIL_SSL_ENABLE="true"

# JWT（必须改成强随机；示例：openssl rand -hex 32）
JWT_SECRET="change-me-to-a-long-random-secret"

# 阿里云 OSS（可选）
ALIYUN_OSS_ENDPOINT="oss-cn-hangzhou.aliyuncs.com"
ALIYUN_OSS_ACCESS_KEY_ID=""
ALIYUN_OSS_ACCESS_KEY_SECRET=""
ALIYUN_OSS_BUCKET_NAME=""
ALIYUN_OSS_CUSTOM_DOMAIN=""
ALIYUN_OSS_DIR_PREFIX="dataelf/"

# 初次上线建议先 false；HTTPS 配好再改 true
REQUIRE_HTTPS="false"

# 后端监听端口（默认 8080）
BACKEND_PORT="8080"

# 前端目录（默认 /opt/dataelf/frontend/dist）
FRONTEND_ROOT="/opt/dataelf/frontend/dist"


